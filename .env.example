# Copy to .env (or set in your environment) before running `npm start`.

# Free Companies House API key — register at:
# https://developer.company-information.service.gov.uk/
# Create an application, then a "REST" / "Live" API key.
CH_API_KEY=

# Port for the web server (default 3000)
PORT=3000
